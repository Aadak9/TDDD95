#include <iostream>
#include <vector>
struct Point
{
    double x;
    double y;
};

int trilemma(const Point& p1, const Point& p2, const Point& p3)
{
    return case;
}

int main()
{
    int N;
    std::cin >> N;
    for (int i = 0; i < N; i++)
    {
        Point p1, p2, p3;
        std::cin >> p1.x >> p1.y >> p2.x >> p2.y >> p3.x >> p3.y;
        int case = trilemma(p1, p2, p3);
        //TODO: switch or if-statements
    }
}